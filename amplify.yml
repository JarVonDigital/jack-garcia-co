version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm i
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist/jack-garcia-co/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
